<resources>

    <!--A style resource file can contain one or more styles. Each style is defined through the <style> element.Each style must have a name,
    which you define with the name attribute; for example:name="AppTheme"-->
    <!--The parent attribute specifies where the style should inherit its properties from. The parent attribute specifies where the style should inherit its properties from.
    The theme determines the basic appearance of the app, such as the color of the app bar and any views.  -->

    <!--You can use customize the look of your app by overriding the properties of an existing theme in the style resource file.-->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--The color resource file is usually called colors.xml, and it’s located in the app/src/main/res/values folder.-->
</resources>
